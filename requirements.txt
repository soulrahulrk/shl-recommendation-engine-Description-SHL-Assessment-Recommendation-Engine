# Lightweight requirements for memory-constrained deployment (< 512MB)
# Use this for free tier deployments (Render, Railway, etc.)

# Core Dependencies (minimal versions)
streamlit>=1.40.0
sentence-transformers>=3.3.0
faiss-cpu>=1.9.0
groq>=0.4.0

# Essential utilities
python-dotenv>=1.0.0
numpy>=2.0.0

# Note: Remove these to save memory on free tier:
# - requests, beautifulsoup4, lxml (only needed for crawling, not runtime)
# - pandas, openpyxl (only needed for data processing, not runtime)
# - fastapi, uvicorn (not needed for Streamlit deployment)
# - tqdm, pymupdf (not needed for runtime)
